# Tabular Premium Signals (legacy label folder)

This folder name is historical. The V3 generation is **Tabular Premium Signals**
(`v3_tabular_premium_signals`), not a generic “next experiments” era.

Active V3 packages and runs live in the parent tree:

- Packages: `../source_versions/` (V19–V24.1; best Kocaeli global = V24.1, best refreshed Başiskele-only = V23)
- Runs: `../outputs/` (gitignored)
- Helpers: `../shared_scripts/`

See [`../README.md`](../README.md). Visual / satellite work belongs under `../../v4/`.
